// Escribe un código que imprima cada elemento del array colores en la consola utilizando un bucle for...of.

let colors = ['blue', 'red', 'violet']

for (let color in colors){
    console.log(color);
};
